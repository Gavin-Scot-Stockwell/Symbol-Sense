name: Deploy To Render

on:
  push:
    branches: [actions]  # Trigger deploy on push to the 'actions' branch
  pull_request:
    branches:
      - actions  # Trigger deploy on pull request targeting 'actions' branch

jobs:
  ci:
    name: Deploy To Render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        if: github.ref == 'refs/heads/actions'  # Make sure the deployment only happens on 'actions'
        env:
          deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          echo "Deploy URL: $deploy_url"  # Debugging step to ensure URL is being set
          curl "$deploy_url"
